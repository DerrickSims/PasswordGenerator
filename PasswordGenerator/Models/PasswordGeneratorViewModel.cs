namespace PasswordGenerator.Models
{
    public class PasswordGeneratorViewModel
    {
        public int PasswordLength { get; set; }
        public string Password { get; set; }    
    }
}
/*
 * Creat a function that generates a random string of charachters and the that function will return that string.
 * in the view creat a question using label and an input to ask the user how long the password will be
 * creat a button the will submit the value and activate the function and 
 * and creat a span to show the result to the user.
 */